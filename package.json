{
  "name": "wa-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "Pont web auto-hébergé vers ton compte WhatsApp (Baileys) — messagerie temps réel : texte, médias, accusés, réactions, groupes, archivage. NestJS + React, dockerisé.",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" run build && pnpm -r --filter \"./apps/*\" run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "shared:build": "pnpm --filter @app/shared-types run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-react-refresh": "^0.4.16",
    "globals": "^15.14.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.19.0"
  }
}
